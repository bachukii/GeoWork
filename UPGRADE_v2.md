# GeoBid v2 — რუკა, გადახდა, ნახაზის მიწოდება

## რა დაემატა

| ნაწილი | აღწერა |
|---|---|
| 🗺️ **რეალური რუკა** | Leaflet + OpenStreetMap. წერტილის მონიშვნა ან ნაკვეთის კონტურის დახაზვა, ფართობის ავტომატური გამოთვლა WGS84-ზე |
| 🧭 **ნავიგაცია** | ამზომველი ხედავს სამუშაოს რუკაზე, მანძილს თავისი ადგილიდან და ერთი დაჭერით მიდის Google/Apple Maps-ში |
| 💳 **გადახდა** | დამკვეთი აფიქსირებს გადახდას → ადმინი ადასტურებს → ფაილები იბლოკება/განიბლოკება |
| 📁 **ნახაზის მიწოდება** | ამზომველი ტვირთავს PDF/DWG/DXF/ZIP-ს, ვერსიებით. კლიენტი იღებს მხოლოდ გადახდის შემდეგ |

---

## 1. მიგრაციის გაშვება (აუცილებელი)

Supabase → **SQL Editor** → **New query** → ჩააკოპირე `supabase/migration_v2.sql` → **Run**

ეს დაამატებს:
- `orders`-ს: `lat`, `lng`, `polygon`, `payment_status`, `paid_amount`, `payment_ref`, `paid_at`
- ახალ ცხრილებს: `payments`, `deliverables`
- Storage bucket-ს `deliverables` (**private**)
- RLS პოლიტიკებს და `confirm_payment()` ფუნქციას

> `schema.sql`-ს ხელახლა **ნუ** გაუშვებ — ის უკვე გაშვებულია. მხოლოდ `migration_v2.sql`.

## 2. კოდის განახლება

ჩაანაცვლე GitHub-ზე:
- `package.json` (დაემატა `leaflet`)
- მთელი `src/` საქაღალდე
- `supabase/migration_v2.sql` (ახალი)

წაშალე: `src/components/AreaDraw.jsx` (შეცვალა `MapPicker.jsx`-მა)

Netlify თავისით აეწყობა push-ის შემდეგ. env ცვლადები არ იცვლება.

---

## 3. როგორ მუშაობს ფაილების დაბლოკვა

ეს ყველაზე მნიშვნელოვანი ნაწილია და **სერვერზეა** გაკეთებული, არა UI-ში.

`storage.objects`-ის SELECT პოლიტიკა კლიენტს ფაილს აძლევს მხოლოდ თუ:
1. ის ამ შეკვეთის დამკვეთია, **და**
2. `orders.payment_status = 'confirmed'`, **და**
3. `deliverables.released = true` (ამზომველმა გაუშვა)

ანუ signed URL-ის მოთხოვნაც კი ჩავარდება, სანამ სამივე პირობა არ სრულდება.
ფაილები private bucket-შია — შემთხვევითი საჯარო URL არ არსებობს.

---

## 4. ტესტირების სცენარი

1. **დამკვეთი** — ახალი შეკვეთა → ნაბიჯი 2 → „რუკაზე მოვნიშნავ" → აირჩიე
   „⬡ ნაკვეთის კონტური" → დააჭირე რუკაზე 4-ჯერ → ფართობი გამოითვლება → გამოაქვეყნე
2. **ამზომველი** — გახსნის შეკვეთას, დაინახავს რუკას, მანძილს, ნავიგაციის ღილაკს →
   გააგზავნის შეთავაზებას
3. **დამკვეთი** — აირჩევს ამზომველს
4. **ამზომველი** — „📁 ნახაზის მიწოდება" → ატვირთავს PDF-ს → „📤 დამკვეთისთვის გაგზავნა"
5. **დამკვეთი** — ფაილების ადგილას ხედავს 🔒 „დაბლოკილია"
6. **დამკვეთი** — „💳 გადავიხადე" → ტრანზაქციის № → გაგზავნა
7. **ადმინი** — „გადახდები" ტაბი → „✓ დადასტურება"
8. **დამკვეთი** — განაახლებს გვერდს → ფაილი გამოჩნდა, ჩამოტვირთვა მუშაობს

---

## ⚠️ რაზე უნდა იცოდე

**გადახდა ხელით დასტურდება.** ბარათით ონლაინ გადახდა (TBC/BOG eCommerce) მოითხოვს
მერჩანტ-ხელშეკრულებას ბანკთან, PCI-ს მოთხოვნების დაცვას და სერვერულ callback-ს —
ეს კოდის დაწერაზე მეტად იურიდიული და საბანკო პროცესია. ამიტომ ახლა მოდელი ასეთია:
დამკვეთი გადარიცხავს, აფიქსირებს ნომერს, ადმინი ადასტურებს. ლოგიკა (ბლოკირება/განბლოკვა)
უკვე სწორად დგას, ბანკის ინტეგრაცია მხოლოდ `confirm_payment()`-ის ავტომატურ
გამოძახებას ჩაანაცვლებს.

**ესქრო (თანხის დაკავება) არ არის.** ეს ცალკე ლიცენზირების საკითხია საქართველოში.

**რუკა OpenStreetMap-ია, არა Google Maps.** OSM უფასოა და გასაღები არ სჭირდება.
Google Maps-ს ბილინგის ჩართვა და საკრედიტო ბარათი სჭირდება. თუ მაინც გინდა Google —
სატელიტური სურათი და ქართული ტოპონიმიკა უკეთესია — მითხარი და გადავიყვან.

**საკადასტრო კოდის lookup კვლავ mock-ია.** NAPR-ის რეალური WFS ჯერ არ არის ჩართული.
ახლა უკვე მნიშვნელოვანია, რადგან რუკაზე ხელით მონიშვნა მუშაობს, კოდით ავტომატური
კი — ჯერ არა.

**ფაილის ლიმიტი 50MB.** Supabase-ის უფასო გეგმაზე Storage 1GB-ია. DWG/DXF-ები
დიდია, ამიტომ თვალი ადევნე მოცულობას.
