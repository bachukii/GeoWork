# ბრენდი და განლაგება

## ⚠️ ფერები — გულახდილად

ლოგოს ფაილი AVIF ფორმატშია და მისი პიქსელები **ვერ წავიკითხე**.
ანუ ეს ფერები GEOID-ის ოფიციალურ პალიტრას **არ არის ამოღებული** — მე ავირჩიე,
შენივე საქმიდან გამომდინარე:

| ცვლადი | ფერი | საიდან |
|---|---|---|
| `--navy` | `#0D2E4E` | გეოდეზიური ხელსაწყოს კორპუსი — სტრუქტურა, სათაურები |
| `--sky` | `#2E7DC2` | მიმდინარე სტატუსი, ბმულები |
| `--gold` | `#F2C300` | საველე ჟილეტი/ნიშნული — მთავარი მოქმედება |
| `--concrete` | `#E9EBE6` | ფონი |

**თუ ზუსტი კოდები გაქვს, გამომიგზავნე ან თვითონ შეცვალე** —
`src/styles.css`-ის დასაწყისში ოთხი ხაზია, სხვაგან არსად არაა ჩაწერილი.

---

## ლოგო

`src/components/Logo.jsx` — SVG, კოდში დახატული (სურათი არაა, არ ბუნდოვანდება).

ნიშანი ტრიანგულაციის სამკუთხედია: გეოდეზიაში ეს საბაზისო ფიგურაა —
სამი წერტილი, სამი ხაზი, ერთი გაზომილი ბაზისი. ამავე დროს სამ მხარეს ნიშნავს:
დამკვეთი, ამზომველი, პლატფორმა.

ტიპოგრაფია GEOID-ის სტრუქტურას იმეორებს: როგორც **GEO**+**ID** ორტონიანია,
ისე **Geo**+**Bid**.

Favicon — `public/favicon.svg`.

---

## განლაგება — ახლა ეკრანს ერგება

| სიგანე | რა ხდება |
|---|---|
| < 900px | ისე როგორც იყო — ვიწრო სვეტი, ქვედა ნავიგაცია |
| ≥ 900px | გვერდითი მენიუ მარცხნივ, ფართო შიგთავსი, ბარათები რამდენიმე სვეტად, ფურცელი გვერდიდან ამოდის |
| ≥ 1200px | საწყისი ეკრანი ორსვეტიანი |

ერთი კოდია — CSS თვითონ გადაერთვება. ცალკე desktop ვერსია არ არსებობს.

---

## ასატვირთი

- `src/styles.css` — ახალი პალიტრა + responsive
- `src/components/Logo.jsx` — ახალი
- `src/App.jsx` — ლოგო სათაურში
- `src/views/AuthScreen.jsx` — საწყისი ეკრანი
- `src/views/ClientView.jsx`, `SurveyorView.jsx`, `AdminView.jsx` — კარკასი
- `index.html` — favicon
- `public/favicon.svg` — ახალი საქაღალდე

SQL არ იცვლება.
